# Packages for `biodiversity_gbif_demo.ipynb` and `gbif_eda.ipynb`

# Core data analysis
pandas>=2.0.0
numpy>=1.24.0

# GBIF API wrapper
pygbif>=0.6.0

# Geospatial
geopandas>=0.13.0

# Plotting
matplotlib>=3.7.0
seaborn>=0.13.0

# ML (used in the demo notebook)
scikit-learn>=1.3.0

# Parquet support (used for caching GBIF results)
pyarrow>=14.0.0

# City -> (lat, lon) geocoding for “Madrid and surroundings” queries
geopy>=2.4.0

# Progress bars for paginated downloads
tqdm>=4.66.0

# Jupyter progress bar integration (optional; removes tqdm IProgress warning)
ipywidgets>=8.1.0

# Interactive maps (basemap) + HTTP client for OSM Overpass download
folium>=0.16.0
requests>=2.31.0
